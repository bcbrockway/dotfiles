[colors]
background = ${xrdb:background:#222}
background-alt = ${xrdb:color0:#222}
foreground = ${xrdb:foreground:#222}
foreground-alt = ${xrdb:color7:#222}
primary = ${xrdb:color1:#222}
secondary = ${xrdb:color2:#222}
alert = ${xrdb:color3:#222}

[bar/taskbar]
bottom = false
width = 100%:-10
height = 28
radius = 0.0
fixed-center = true

override-redirect = true
offset-x = 5
offset-y = 5

background = ${colors.background}
foreground = ${colors.foreground}

line-size = 3
line-color = #f00

border-size = 0
border-color = #00000000

padding-left = 0
padding-right = 4

module-margin-left = 2
module-margin-right = 2

font-0 = "UbuntuMono Nerd Font:style=Regular:size=11:antialias=true;2"

modules-left = i3
modules-center = date
modules-right = cpu memory filesystem wifi

tray-position = right
tray-padding = 2

scroll-up = i3wm-wsprev
scroll-down = i3wm-wsnext

[module/i3]
type = internal/i3

; Only show workspaces on the monitors they're assigned to
pin-workspaces = true

; Sort workspaces by index
index-sort = true

; Allow clicking to select a workspace
enable-click = true

; Disable scrolling (we've enabled this on the whole bar above)
enable-scroll = false
wrapping-scroll = false
reverse-scroll = false

format = <label-state> <label-mode>

label-mode = %mode%
label-mode-foreground = ${colors.foreground}
label-mode-background = ${colors.primary}

label-focused = %name%
label-focused-background = ${colors.background-alt}
label-focused-padding = 1

label-unfocused = %name%
label-unfocused-padding = 1

label-visible = V %index%
label-visible-background = ${self.label-focused-background}
label-visible-underline = ${self.label-focused-background}
label-visible-padding = ${self.label-focused-padding}

label-urgent = %name%
label-urgent-background = ${colors.alert}
label-urgent-padding = 1

[module/date]
type = internal/date
interval = 1

date = "%a %d/%m/%Y"

time = %H:%M:%S

label = %date% %time%

[module/cpu]
type = internal/cpu
interval = 2

label =  %percentage:3%%

format = <label>

[module/filesystem]
type = internal/fs

mount-0 = /

label-mounted =  %used:6% / %total:6%

[module/memory]
type = internal/memory
interval = 2

label =  %gb_used:7% / %gb_total:8%

[module/wifi]
type = internal/network

{% if yadm.hostname == "LON-LX-L-671" %}
interface = wlp59s0
{% else %}
interface = wlp3s0
{% endif %}
interval = 2

label-connected = U:%upspeed:8% D:%downspeed:8%

ramp-signal-0 = ""
ramp-signal-1 = ""
ramp-signal-2 = ""
ramp-signal-3 = ""
ramp-signal-4 = ""

format-connected = <ramp-signal>  <label-connected>

; vim:ft=dosini
